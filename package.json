{
  "name": "coms-backend",
  "version": "1.0.0",
  "description": "A Compliance Management System",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./src/main.js",
    "start": "node ./src/main.js",
    "migrate": "env $(cat .env) knex migrate:latest --knexfile <knexfile>",
    "rollback": "env $(cat .env) knex migrate:rollback --knexfile <knexfile>"
  },
  "author": "Fullsuite",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "knex": "^3.1.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
